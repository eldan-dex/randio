ISSUES:
- player texture is not moving along with player (?!)
- block texture is drawn across the whole screen (?!)


IDEAS:
- Tile blending - for each tile, blend it's edges into surrounding tiles' textures -> keeps tile textures spearated, but blended